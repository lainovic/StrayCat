package com.lainovic.tomtom.straycat.ui.theme

import androidx.compose.ui.graphics.Color

// Light mode inspired color palette
object AppColors {
    // Surface colors
    val Surface = Color(0xFFFFFFFF) // Pure white

    // Primary colors - TomTom brand blue
    val Primary = Color(0xFF0077C8) // TomTom blue
    val PrimaryDisabled = Color(0xFFBDBDBD) // Light gray
    val OnPrimary = Color.White

    // Error colors
    val Error = Color(0xFFFF5252) // Bright red
    val OnError = Color.White

    // Disabled state
    val OnDisabled = Color(0xFF9E9E9E)

    // Progress indicator
    val Progress = Color(0xFFB3D9F2) // Light TomTom blue
    val ProgressTrack = Color(0xFFF5F5F5) // Very light gray
}

